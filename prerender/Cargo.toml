[package]
name = "prerender"
version = "0.1.0"
edition = "2024"

[dependencies]
site = { path = "../site", features = ["ssr"] }
leptos = { version = "0.8.20", default-features = false, features = ["ssr"] }
